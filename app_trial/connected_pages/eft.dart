import 'package:flutter/material.dart';

class eft extends StatefulWidget {
  const eft({Key? key}) : super(key: key);

  @override
  State<eft> createState() => _eftState();
}

class _eftState extends State<eft> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("eft", style: TextStyle(fontSize: 69)));
  }
}
